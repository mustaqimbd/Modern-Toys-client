import App from '../App';
import Navbar from '../SharedComponents/Navbar';

const MainLayout = () => {
    return (
        <div>
            <Navbar />
        </div>
    );
};

export default MainLayout;